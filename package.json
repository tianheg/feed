{
  "name": "feed",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "concurrently \"pnpm:build:*\"",
    "build:life": "pnpm --prefix src/life build",
    "build:tech": "pnpm --prefix src/tech build",
    "style": "gulp css",
    "watch": "gulp watch",
    "copy": "node copy.mjs"
  },
  "workspaces": [
    "./src/*"
  ],
  "devDependencies": {
    "@osmoscraft/osmosfeed": "^1.4.0",
    "concurrently": "^7.2.1",
    "fs-extra": "^10.1.0",
    "gulp": "^4.0.2",
    "gulp-sass": "^5.1.0",
    "sass": "^1.52.1"
  }
}